# Workflow

Placeholder. Populated in Phase 7 with a long-form narrative tutorial: a
stranger should be able to install tea-eyes, run through a full
render → look → reason → edit → re-render loop, and write a golden test,
without external help.
