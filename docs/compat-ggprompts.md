# Compatibility with the GGPrompts / TFE bubbletea skill

Placeholder. Populated in Phase 4.

**Short version:** tea-eyes does **not** duplicate the layout rules encoded
in the GGPrompts/TFE bubbletea Claude Code skill (the "4 Golden Rules":
account for borders, never auto-wrap in bordered panels, match mouse
detection to layout, use weights not pixels). tea-eyes is *additive* —
install both for the full design loop. The `tea-eyes-bubbletea` skill
explicitly defers to GGPrompts for all layout guidance.
